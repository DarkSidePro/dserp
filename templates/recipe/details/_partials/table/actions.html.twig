<div class="btn-group ml-auto">
    <a href="{{ path('recipe_detail_update', {'id': row.id }) }}" class="btn btn-sm btn-outline-light">Edit</a>
    <a href="{{ path('recipe_detail_remove', {'id': row.id }) }}" class="btn btn-sm btn-outline-light remove">
        <i class="far fa-trash-alt"></i>
    </a>
</div>